# Heavy ML deps — Cloud Run ONLY (installed via cloudrun.Dockerfile).
# Railway does NOT install this file; its Nixpacks builder only picks up
# requirements.txt, so these stay off Railway's small tier.
# app.py lazy-imports these modules, so the app still boots without them.
# Pin TensorFlow/Pillow so a Cloud Run rebuild does not silently change Keras behavior.
tensorflow-cpu==2.15.0
pillow==10.4.0
