# Lightweight deps — always installed.
# Railway (Nixpacks) installs ONLY this file, so it stays small enough for
# Railway's tier (no TensorFlow, no XGBoost).
# Cloud Run's Dockerfile installs this PLUS requirements-ml.txt.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pydantic>=2.5.0
